-- REVIEW DATA QUALITY AFTER LOADING THE CLEANED AND TRANSFORMED DATA INTO silver.erp_cust_az12
-- Identify future dates and out of range dates for bdate column
		SELECT DISTINCT bdate
		FROM silver.erp_cust_az12
		WHERE bdate < '1924-01-01' OR bdate > GETDATE();

-- Check gen column inconsistency and standarization
		SELECT DISTINCT gen FROM silver.erp_cust_az12;

-- Final look at table silver.erp_cust_az12
		SELECT * FROM silver.erp_cust_az12;
